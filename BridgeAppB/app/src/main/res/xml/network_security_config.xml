<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuração de segurança de rede.
    Permite tráfego HTTP (cleartext) específicamente para localhost.
    Android 9+ bloqueia cleartext HTTP por defeito — esta config é obrigatória
    para que o WebView do Power Apps consiga aceder a http://localhost:8080.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
